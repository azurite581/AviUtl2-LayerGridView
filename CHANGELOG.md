# Changelog

## [Unreleased]

## [0.2.0] - 2026-09-20

> [!Caution]
> ### Breaking Changes
>
> - セル毎に個別オブジェクトをオプション項目に変更。  
  0.1.0 ではデフォルトでセル毎に個別オブジェクトが有効でしたが、0.2.0 からオプション（チェック項目）となり、有効/無効が切り替えられるようになりました。  
  初期状態では無効（チェック無し）になっているため、0.1.0 に合わせる場合は、有効化する（チェックを入れる）必要があります。

### Changed

- 一部設定項目に非表示条件を追加。
  - `セル色` は `セル色を上書き` が有効なとき、`領域外の色` は `領域外` が `指定色` のときのみ表示されます。
- `--hide` の使用に伴い要求バージョンを 2.1.8 に引き上げ。
- `スクリーンサイズに合わせる` オプションの初期値を無効に変更。
- `読み込み` を `レイヤー上のオブジェクトを読み込む` に変更。

### Fixed

- 個別オブジェクト有効時に、アンカー枠のサイズがグリッドサイズと同じになるように修正。



## [0.1.0] - 2026-08-02

### Added

- 初版。
